# RPA 回调 vid=0 问题反馈报告

**日期**: 2026-08-28
**状态**: 待企销宝确认

---

## 问题概述

企销宝回调消息中,有一条消息的 **`vid` 字段为 0**(企微号缺失),导致:
1. 回发客户时 `send_text_message(vid=0, ...)` 被企销宝拒绝
2. 企销宝返回 **`code=422 message="企微号不存在"`**

## 证据

### 数据库落库数据

**会话 `9012581`**(2026-08-28 14:03:43 创建):

| 字段 | 值 |
|---|---|
| conversation_id | `9012581` |
| customer_id | `1688850037545792` ← **这是企微号,不是客户!** |
| **vid** | **`0`** ← **企微号缺失!** |

**客户表**同时间记录:
```
customer_id = 1688850037545792
vid         = 0
```

### 回调处理日志

```
14:03:43  POST /callback/1  200 OK     ← 这条消息进来
14:03:50  message/send → code=422 "企微号不存在"   ← 回发被拒
```

### 回发报错

```
ERROR app.service RPA 回发失败(已落库): 发送失败 http=200 code=422 msg=企微号不存在
```

## 分析

报文结构推断(根据落库的 customer_id/vid):

```json
{
  "json": {
    "sender": "1688850037545792",   ← 填成了企微号
    "vid": 0,                        ← vid 字段缺失/为 0
    ...
  }
}
```

**问题**: 企销宝回调报文中 `sender` 填的是企微号,但 `vid`(企微号字段)缺失为 0。

对比正常消息(会话 `9012524`):
| 字段 | 正常消息 | 异常消息(9012581) |
|---|---|---|
| sender(customer_id) | `7881300986104141`(客户) | `1688850037545792`(企微号) |
| vid | `1688850037545792`(企微号) | `0` |

## 请求确认

1. 回调报文中 **`vid` 字段的语义**是什么?为什么这条消息 `vid=0`?
2. 回调报文中 **`sender` 到底是客户还是企微号**?正常应该是客户 ID。
3. 是否企销宝侧配置问题导致这条消息的 `vid` 未填充?

## 影响

- 该消息**AI 已正常回复并落库**,但**无法回发到客户微信**(企销宝 422 拒绝)
- 后续同类消息若 `vid=0`,回发都会失败
